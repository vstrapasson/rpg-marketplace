---
description: How to use rpg-gamemaster — a session prep and run layer that turns a built Pathfinder 2e campaign into runnable sessions, running session zero, advancing the world between games, and closing the loop after a session, through eight chained skills and the vault as shared memory.
---

Present this guide to the user conversationally — walk them through what the kit is for and the workflows below — then offer to start the workflow that best fits where they are (running session zero to build the party, setting up a front before prep, running prep, building an encounter or a non-combat challenge, sitting down to run, or closing out a session that just happened).

# What it is for

You have a world — regions, factions, a clue map, a campaign bible built with the loremaster kit — and now you need to turn that content into something you can actually run at the table. This kit is the **session prep and run layer**: it runs the **session zero** that builds the player characters and captures the party, then reads what the world has built, advances the living threats between sessions, assembles a one-page session plan, balances the encounter math, structures the non-combat challenges, produces the GM overlay you glance at during play, and — after the session ends — reconciles what happened back into the vault so the cycle can repeat. Eight skills, a clear flow, and one vault as shared memory. No worldbuilding happens here; if a needed node doesn't exist yet, the skill points you back to the loremaster kit to build it first.

# Quick start

1. Install the plugin: `/plugin marketplace add ~/projects/rpg-marketplace` (or the GitHub URL), then `/plugin install rpg-gamemaster@rpg-marketplace`, then `/reload-plugins`.
2. Confirm the eight `rpg-*` skills appear with `/skills`.
3. For persistence, `rpg-vault-guardian` must also be installed — the gamemaster skills write all vault entities through the `rpg-preserve` write gate, which the guardian provides. Without it, the skills can still produce output; they just won't persist to disk.
4. The typical flow: **party-forge (session zero, once) → front-tracker → session-prep → encounter-builder / embate-builder → run-sheet → [play] → session-recap → front-tracker → …**. Run them in that order for a full campaign, or invoke any skill alone when you have a specific need.

# What you can do

| How to invoke | What it does | Reach for it when |
| --- | --- | --- |
| `"Run session zero"` / `"build the party"` / `"criar os personagens dos jogadores"` / `"montar o grupo"` / `"create the PCs"` (skill `rpg-party-forge`) | The players' side. Runs a session zero and builds each player character — concept, a core belief and an active wound, want vs. need, a contradiction, a table voice — then anchors every PC to the world with a hook (a faction/region/location/front/clue by exact vault name) and an authored GM-secret convergence layer that crosses the other PCs' threads. Captures party level/size in a loose party overview the builders read. Persists each PC as a `jogador` entity via `rpg-preserve`; offers a player-safe handout. | Session zero, before the campaign runs. When players bring concepts you want deepened and wired into the world. When you want party level/size captured once instead of re-asked. |
| `"Advance the front"` / `"what did the villains do since last session?"` / `"tick the clock"` / `"set up a front"` (skill `rpg-front-tracker`) | The living-world engine. Bootstraps a front from a faction's plan-in-motion (its grim portents and impending doom) and advances those clocks based on what happened last session — then emits the "what the world did since last session" beat that feeds the session-prep strong start. Persists `frente` and `relogio` vault entities via `rpg-preserve`. | Between sessions, before you prep. When you want the world to feel like it moves without the players. When a faction's authored plan needs to become a trackable, advancing threat. |
| `"Prep the next session"` / `"what should happen next session?"` / `"montar a sessão"` / `"qual o gancho"` (skill `rpg-session-prep`) | The session launchpad. Reads the campaign bible, the clue map, the active fronts and clocks, and the vault entities in play, then assembles a one-page session plan using the Lazy DM eight steps — weighted on secrets and clues as the spine. Derives the strong start from what the world did since last session; carries forward unrevealed secrets from the clue map rather than starting fresh. Persists a `sessao` vault entity via `rpg-preserve`. | When you are preparing an upcoming session and want a structured, improvisation-ready one-pager built from what already exists in the vault. |
| `"Build an encounter"` / `"balance this fight"` / `"montar um combate"` / `"quantos inimigos"` (skill `rpg-encounter-builder`) | The mechanical layer. Given party level, party size, and a scene or threat brief, it picks a threat tier, selects creatures from the Archives of Nethys, shows the XP-budget arithmetic, adds hazards and treasure by level, and wraps the whole thing as a situation — an objective, a terrain feature, the reason the fight exists, and how it escalates or ends. Always shows the math. Persists an `encontro` vault entity via `rpg-preserve`. | When session-prep has flagged a threat that needs balancing, or when you want to build a specific encounter from scratch. |
| `"Build a challenge"` / `"desafio social"` / `"montar uma perseguição/infiltração/pesquisa"` / `"prova de acrobacia/intuição/intimidação"` (skill `rpg-embate-builder`) | The non-combat mechanical layer — the sibling of the encounter builder for everything resolved by skills, not swords. Scalable: a single dramatic check (DC + degrees + fail-forward) or a full Victory Points challenge with the four PF2e subsystem presets (Influence, Chase, Research, Infiltration). Sizes the VP target and thresholds against the official scale, anchors DCs to the Level-Based and Simple DC tables, and wraps the scene as a situation that measures a world-state with fail-forward consequences. Always shows the math. Persists a `desafio` vault entity via `rpg-preserve`. | When a scene is a negotiation, a chase, a heist, a research montage, or a single tense skill test — not a fight. |
| `"Itemize the treasure"` / `"distribuir o tesouro do nível"` / `"montar o butim"` / `"quanto de tesouro"` / `"are my players at the right wealth"` (skill `rpg-treasure-builder`) | The loot layer. Turns a PF2e treasure budget (one encounter's allotment, a whole level, or a named gp value) into concrete, itemized loot — permanent items, consumables, currency, gems, art by item level, referenced to Archives of Nethys category pages, with the arithmetic shown and the party-size adjustment applied. Notable items become `item` entities (value/level/rarity/category) via `rpg-preserve`; bulk currency stays aggregate. Maintains a `## Wealth` section in the party overview (expected vs. awarded). Relics stay with `rpg-artifact-creator`. | Distributing a hoard or the level's loot; checking the party against the wealth-by-level expectation. Hand off to `rpg-treasure-forge` (foundry-forge) to sync the players' actual sheets. |
| `"Make a run sheet"` / `"folha de mesa"` / `"o que acompanhar durante o jogo"` / `"rodar a sessao"` (skill `rpg-gm-run-sheet`) | The table overlay. Reads the session-prep one-pager, the clue map's revelation IDs, and the active fronts and clocks, then compresses them into one printable page — strong start, scene beats checklist, secrets-and-clues checklist keyed to R-IDs, NPC quick-refs with a voice line and a want, the active clocks, and the "if they stall" backstops. Writes nothing to the vault; the run sheet is an ephemeral printable artifact. | Sitting down to run. Gives you the GM overlay that a VTT does not — narrative and discovery state only, no initiative or dice. |
| `"Recap the session"` / `"o que aconteceu na sessão"` / `"atualizar o vault depois do jogo"` / `"fechar a sessão"` (skill `rpg-session-recap`) | The return edge. Takes the GM's post-session account (plus an optional linked transcript) and reconciles it against the vault: updates the `sessao` note with outcomes, creates `evento` entities, flips `quest.status`, marks clue-map revelation IDs revealed, and records NPC/faction state shifts. Where the account conflicts with vault or bible state — an unresolved name, an ambiguous outcome, a fact that breaks canon — it stops and asks which path to take, batches the unambiguous changes for one approval, and persists everything through `rpg-preserve`'s body-preserving update path so existing note prose survives. | Right after a session ends. Closes the loop so `rpg-front-tracker` and `rpg-session-prep` have real state to work from instead of the GM re-feeding them by hand. |

# Workflows

## Session zero — build the party before the campaign runs (once)

1. Make sure the world has enough to anchor into: the campaign bible (tone, central truth, antagonist) and at least the starting region, settlement, and the antagonist's faction. If a hook target is missing, party-forge emits a loremaster handoff to build it.
2. Trigger `rpg-party-forge` ("run session zero — let's build the party"). It settles the table-level decisions once (system, party level and size, lines and veils), then builds each PC: it draws out the core (belief + active wound), the want vs. need, the table voice, and — the part that makes it a *party* — anchors each character to a real vault node by a hook and authors a GM-only secret/convergence layer that crosses the other PCs.
3. Each PC is persisted as a `jogador` entity via `rpg-preserve`. Party level/size land in a loose `party-<slug>.md` overview that `rpg-encounter-builder` and `rpg-embate-builder` read instead of asking you every time.
4. Offer the handoffs: `rpg-ownership-forge` (Foundry actor ownership), `rpg-clue-mapper` (each PC's secret as a revelation), and a player-safe handout with the secrets stripped.

## The full session cycle — from a living world to the table and back

1. Confirm the eight `rpg-*` skills are installed with `/skills`.
2. Before prep, advance the world: tell `rpg-front-tracker` to tick the relevant clocks based on what happened last session ("advance the front — the Syndicate kept moving while the party was traveling"). It asks which front, reads the faction's grim portents and the last session note, and advances the clocks. Its "what the world did since last session" beat is the seed for the strong start.
3. Run `rpg-session-prep` ("prep the next session"). It reads the campaign bible, the clue map (carrying forward unrevealed revelation IDs), the active fronts' "next move" beat, and the vault entities in play. It assembles a one-page session plan: a strong start derived from the world's state, three to five likely scenes, the secrets and clues live this session, the NPCs and threats in play by vault name, rewards, and backstops. If a scene needs an encounter balanced, it emits a handoff to `rpg-encounter-builder`.
4. When `rpg-session-prep` flags a fight that needs math, run `rpg-encounter-builder`. Give it the party level, party size, and the threat brief from the session plan. It picks a tier, selects creatures (AoN category refs, XP arithmetic shown), wraps it as a situation, and persists an `encontro` entity.
5. When a scene is resolved by skills rather than swords — a negotiation, a chase, a heist, a research montage, a single tense check — run `rpg-embate-builder` instead. Give it party level and size and the scene brief. It picks the subsystem (or a single check), sizes the Victory Points target and thresholds, anchors the DCs, wraps it as a fail-forward situation, and persists a `desafio` entity.
6. Sitting down to run, ask `rpg-gm-run-sheet` to assemble the run sheet from the session plan and the vault. You get one printable page with everything you need at the table. Save it to the working folder and run.
7. After the session, run `rpg-session-recap` with your account of what happened (paste notes, dictate a summary, or point it at a linked transcript). It reconciles the account against the vault, asks you to adjudicate any divergence, and — once you approve the batch — updates the `sessao`, creates any new `evento` entities, flips `quest.status`, and ticks the clue map. This is the input the next `rpg-front-tracker` run needs.
8. Advance the clocks again with `rpg-front-tracker`, now reading the reconciled `sessao` from step 7 instead of your memory of what happened. The updated `relogio` entities become the input for the next `rpg-session-prep` run — and the cycle repeats from step 2.

## Set up a front for a new faction threat — turn a loremaster-authored faction plan into a trackable, advancing danger

1. Make sure the faction's `faccao` note exists in the vault (built with the loremaster kit's `rpg-faction-creator`) and that it includes the plan-in-motion, grim portents, and impending doom.
2. Trigger `rpg-front-tracker` ("set up a front for this faction"). It reads the `faccao` note and the campaign bible, builds the front's impulse and grim portents as clock segments, sizes the clock (4 segments for a contained danger, 6 for a campaign-level threat, 8 for an existential arc), and persists the `frente` + `relogio` entities via `rpg-preserve`.
3. If the faction's plan has no portents yet, `rpg-front-tracker` will emit a handoff to `rpg-faction-creator` (loremaster) to author the plan first — then come back.
4. The front sheet ends with the "what the world did since last session" beat that feeds into `rpg-session-prep`'s strong start.

## Build an encounter from a session-prep handoff — turn a flagged threat into balanced PF2e numbers, wrapped as a situation

1. When `rpg-session-prep` emits "→ rpg-encounter-builder" for a scene, copy that handoff line and trigger `rpg-encounter-builder` with it.
2. Confirm party level and party size — the skill will ask if they're not in the handoff. Without them, the XP budget is meaningless.
3. The skill picks a threat tier (or you name one), selects creatures by AoN category page (never a `?ID=` link), shows the running XP arithmetic against the budget, adds hazards and treasure by level, and wraps everything as a situation — the objective, a terrain feature, the reason the fight exists, and how it escalates or ends. The math is always shown.
4. The completed encounter is persisted as an `encontro` entity via `rpg-preserve` and handed back to the session plan as a scene.

## Build a non-combat challenge — turn a social/chase/research/infiltration scene into a Victory Points subsystem, wrapped as a situation

1. Decide the shape with `rpg-embate-builder`: a **single dramatic check** (one DC, the four degrees, a fail-forward consequence) or a **structured VP challenge** (a scene resolved over many rolls). The skill reads it from the stakes and offers its read if you're unsure.
2. Confirm party level and party size — DCs are meaningless without them. Pick the subsystem: Influence (social), Chase, Research, Infiltration, or generic.
3. The skill sizes the VP target and thresholds against the official scale (quick 3–5, long 7–10, most-of-a-session 15–25, adventure 25–50), anchors every DC to the Level-Based or Simple DC tables, and shows the VP arithmetic. It then wraps the scene as a situation that **measures a world-state** (the council's favor, the pursuer's distance), describes the situation rather than a fixed skill list, and makes failure **fail forward** — a complication or a new path, never a dead end.
4. The challenge is persisted as a `desafio` entity via `rpg-preserve` (optionally with a `relogio` clock for the VP track). From there, `rpg-foundry-forge`'s `rpg-embate-forge` can realize it in Foundry — a challenge journal, the live rolls, and the VP clock.

## Close the loop after a session — reconcile what happened before you prep the next one

1. Right after the session ends, run `rpg-session-recap` ("recap the session" / "o que aconteceu na sessão"). Give it your account of what happened — paste notes, dictate a summary, or point it at a linked transcript if one exists (`sessao.transcript`). A transcript enriches and verifies; it's never required.
2. The skill extracts the session's beats and matches every named NPC, faction, location, and quest to the vault by exact canonical name. It reads the campaign bible and the clue map for context.
3. It drafts a **change set**: updates to the `sessao` note, new `evento` entities, `quest.status` flips, clue-map revelations, and NPC/faction state shifts — split into a **clean batch** (no contradiction, approve together) and **conflicts** (adjudicated one at a time).
4. Where the account disagrees with what the vault or the campaign bible currently holds — an unresolved name, an ambiguous outcome, a fact that breaks canon — it stops and asks you which path to take, offering concrete options rather than guessing. Your decision is recorded, including a note for the campaign bible's Deliberate Exceptions section if it affects canon.
5. Once you approve, everything persists through `rpg-preserve` — using its **body-preserving update path** for existing notes, so a `quest`'s description or an `npc`'s dossier prose survives the status change intact.
6. The recap ends with handoffs: to `rpg-front-tracker` (advance the clocks from what just happened) and `rpg-session-prep` (plan the next session from the reconciled state), plus any loremaster creators needed for nodes the session introduced.

# Good to know

- **The only slash command this plugin ships is `/rpg-gamemaster-help` (this guide).** All eight skills are invoked by natural language. Confirm them with `/skills`; the other slash interactions you'll use are the install commands (`/plugin`, `/reload-plugins`) and `/skills`.
- **`rpg-vault-guardian` is the persistence prerequisite.** The gamemaster skills write vault entities exclusively through the `rpg-preserve` write gate — a skill the guardian provides. Install `rpg-vault-guardian` for persistence; without it, the skills still produce output but won't write to disk.
- **The vault is the shared memory between plugins.** The gamemaster kit communicates with the loremaster kit by reading vault entities and loose files (campaign bible, clue map) — never by importing code from the other plugin. If a vault node is missing, the skill emits a loremaster handoff and waits; it does not invent content.
- **Four new vault entity types, plus two it authors.** `rpg-front-tracker` writes `frente` (a front — impulse, doom, status) and `relogio` (a progress clock — segments and filled count). `rpg-encounter-builder` writes `encontro` (an encounter — threat tier, party level/size, creature refs). `rpg-embate-builder` writes `desafio` (a non-combat challenge — subsystem, VP format, scale, VP target, party level). These types are defined in `rpg-vault-guardian`'s schema and live in `frentes/`, `relogios/`, `encontros/`, and `desafios/` folders. `rpg-party-forge` authors `jogador` (player characters, in `jogadores/`) and `rpg-treasure-builder` authors `item` (loot, in `itens/`) — neither is a *new* type (both predate the kit and already have their folders), but these skills are the first to write them. As of guardian 1.3.0, `item` carries structured loot fields (value, item_level, rarity, category) for wealth tracking. `rpg-session-recap` is the first skill to *update* existing entities (`sessao`, `quest`, `npc`, `faccao`) rather than only create new ones — it uses `rpg-preserve`'s body-preserving update path so existing note bodies survive.
- **`rpg-gm-run-sheet` writes nothing.** It is the exception — an ephemeral printable artifact saved as `run-sheet-<slug>.md` in the working folder, not routed through `rpg-preserve` and not a canonical vault entity. When the session ends, `rpg-session-recap` (the reconciled `sessao` and `evento` notes) is the canonical record.
- **Secrets and clues is the spine of session prep.** Of the Lazy DM eight steps, secrets and clues is the one to keep if you keep only one. `rpg-session-prep` wires to the clue map's revelation IDs (R1, R2…) and carries forward unrevealed secrets from session to session — never starting fresh, never dropping a revelation just because the players missed it. `rpg-session-recap` is what actually ticks a revelation as revealed, with the GM's confirmation.
- **The encounter math is always shown.** `rpg-encounter-builder` states the assumed party level and size on every output and shows every XP line in the arithmetic. Creature references are Archives of Nethys category pages (e.g. `/Monsters.aspx`) — never `?ID=` deep links.
- **Default tone is dark-leaning (level 3 of 5)** — heroic fantasy with a shadowy edge; not grimdark, not noblebright. All eight skills inherit the tone from the campaign bible; they do not reset it. An explicit tone override in the bible is honored; the run sheet follows the campaign's register.
- **Language matching.** Skills generate in the language you write in (PT-BR in → PT-BR out) while keeping PF2e canon names and AoN URLs in English as canon anchors. Vault folder names and entity type values are Portuguese by convention (`frente`, `relogio`, `encontro`, `desafio`, `sessao`, `frentes/`, `relogios/`, `encontros/`, `desafios/`).
- **Divergence is never resolved silently.** `rpg-session-recap` is the one skill in the kit designed to *stop and ask* — when a GM's account contradicts the vault or the campaign bible, it surfaces the conflict with concrete options instead of guessing which reading is "probably right."
